# Copy this file to .env.local and replace with your Google Maps API key
# The key must have access to the Map Tiles API for Photorealistic 3D Tiles.
VITE_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# Optional: URL to a GLB/GLTF you want to render instead of the default ellipsoid drone marker.
# Example (place your file in src/web/public/models/drone.glb):
# VITE_DRONE_MODEL_URL=/models/drone.glb
VITE_DRONE_MODEL_URL=
